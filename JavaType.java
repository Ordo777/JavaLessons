package JavaLessons;

public class JavaType {
	// примитивные тип
 byte b = 1; //1 байт(256), от -128 до +128
 short s; // 2 байта(2 в 15 степени), от -32.000 до +32.000
 int i; // 4 байта. от -2 миллиарда до +2 
 long l; // 8 байта Дохуя 
 float f;// 4 байта дробный тип памяти, f в конце если ругается.
 double d; // 8 байт тоже дробный тип памяти.
 boolean bo; // true или false.
 char c; // 1 символ.
  // сслочный тип
  Human friend = new Human();
  Human mom;
  Human dad;
  
	 
}
